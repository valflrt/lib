{
  "name": "@valflrt/lib",
  "version": "0.0.4",
  "description": "Some useful things.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "test": "npx ts-mocha -p tsconfig.json test/**/*.test.ts",
    "docs:gen": "rm -rf docs && npx typedoc src"
  },
  "author": "valflrt",
  "license": "MIT",
  "dependencies": {
    "stream": "^0.0.2",
    "typedoc": "^0.23.7"
  },
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.0.3",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.7.4"
  }
}
